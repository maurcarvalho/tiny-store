// Base repository is no longer needed with Drizzle ORM.
// Repositories use the drizzle query builder directly.
// Kept as empty marker for backwards-compatible imports.
export class BaseRepository {}
